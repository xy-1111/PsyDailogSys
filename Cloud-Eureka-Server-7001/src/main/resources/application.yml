server:
  port: 7001

eureka:
  instance:
    hostname: localhost
  client:

    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

#
#spring:
#  datasource:
#    url: jdbc:mysql://localhost:3306/psy_dailog_sys?useUnicode=true&characterEncoding=utf-8&useSSL=false
#    username: root
#    password: 19960601
#    driver-class-name: com.mysql.cj.jdbc.Driver
#  application:
#    name: eureka7001

